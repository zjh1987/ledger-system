version: "3.8"

services:
  ledger-system:
    build: .
    container_name: ledger-system
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/tz.db
      - SKIP_SAMPLE_DATA=1
    restart: unless-stopped

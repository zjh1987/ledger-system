# 安全策略 (Security Policy)

## 支持的版本

| 版本 | 安全修复支持 |
|------|:---:|
| 2.x（当前） | ✅ |

## 报告漏洞

如发现可能导致信息泄露、未授权访问或其他安全问题的缺陷，**请勿在公开的 Issue 中直接披露漏洞细节**。

请通过以下任一私密渠道联系维护者：
- 私信仓库管理员
- 通过 GitHub Security Advisory 的 "Report a vulnerability" 入口提交

请在报告中提供：
- 漏洞类型与影响范围
- 复现步骤（尽量最小化）
- 受影响的版本与可能的修复建议

## 部署安全提醒

- 首次部署后请**立即修改默认管理员密码**（系统已强制引导首次改密）。
- 生产环境设置 `FLASK_DEBUG=0`（默认关闭）。
- 如需跨域访问，通过 `ALLOWED_ORIGINS` 白名单配置，不要全放开。
- 数据库文件 `tz.db` 请做好备份与权限控制。